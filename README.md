### API for stable diffusion image generation from request

## You need commands:
python -m venv env 
env\Scripts\activate    
pip install -r requirements.txt
pip install -r dev-requirements.txt 

uvicorn main:app  
uvicorn main:app --reload


